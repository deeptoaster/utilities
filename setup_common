sudo n latest
sudo npm install -g autoprefixer eslint eslint-config-airbnb eslint-config-prettier npm postcss postcss-cli prettier pyright typescript
sudo mv $DOCUMENT_ROOT/* ~/Public
sudo rmdir $DOCUMENT_ROOT
sudo ln -fns ~/Public $DOCUMENT_ROOT
chmod o+rx ~
sudo apachectl restart
ssh-keygen -C deeptoaster@gmail.com -t ed25519
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
git config --global commit.gpgSign true
git config --global core.excludesFile ~/.gitignore
git config --global diff.tool vimdiff
git config --global gpg.format ssh
git config --global gpg.ssh.allowedSignersFile ~/.ssh/allowed_signers
git config --global pull.rebase false
git config --global user.email deeptoaster@gmail.com
git config --global user.name 'Deep Toaster'
git config --global user.signingkey ~/.ssh/id_ed25519.pub
touch ~/.ssh/allowed_signers
ln -fs $(pwd)/config/bash_aliases ~/.bash_aliases
ln -fs $(pwd)/config/eslintrc.json ~/.eslintrc.json
ln -fs $(pwd)/config/face ~/.face
ln -fs $(pwd)/config/gitignore ~/.gitignore
ln -fs $(pwd)/config/latexindent.yaml ~/latexindent.yaml
ln -fs $(pwd)/config/pam_environment ~/.pam_environment
ln -fs $(pwd)/config/pyrightconfig.json ~/pyrightconfig.json
ln -fs $(pwd)/config/vimrc ~/.vimrc
mkdir -p ~/.lua/scripts
curl -fLo ~/.lua/scripts/terminal.lua -S https://raw.githubusercontent.com/Orange-OpenSource/pandoc-terminal-writer/master/terminal.lua
git clone https://github.com/dense-analysis/ale ~/.vim/pack/foo/start/ale
git clone https://github.com/github/copilot.vim ~/.vim/pack/foo/start/copilot
git clone https://github.com/ctrlpvim/ctrlp.vim ~/.vim/pack/foo/start/ctrlp
git clone https://github.com/preservim/nerdtree ~/.vim/pack/foo/opt/nerdtree
git clone https://github.com/Xuyuanp/nerdtree-git-plugin ~/.vim/pack/foo/opt/nerdtree-git-plugin
git clone https://github.com/vim-airline/vim-airline ~/.vim/pack/foo/start/vim-airline
git clone https://github.com/vim-airline/vim-airline-themes ~/.vim/pack/foo/start/vim-airline-themes
git clone https://github.com/ryanoasis/vim-devicons ~/.vim/pack/foo/opt/vim-devicons
git clone https://github.com/tpope/vim-fugitive ~/.vim/pack/foo/start/vim-fugitive
git clone https://github.com/airblade/vim-gitgutter ~/.vim/pack/foo/start/vim-gitgutter
git clone https://github.com/tpope/vim-surround ~/.vim/pack/foo/start/vim-surround
git clone https://github.com/lervag/vimtex ~/.vim/pack/foo/start/vimtex
mkdir -p ~/.vim/after/syntax ~/.vim/colors ~/.vim/plugin
curl -fLo ~/.vim/after/syntax/python.vim -S https://raw.githubusercontent.com/ehamberg/vim-cute-python/moresymbols/after/syntax/python.vim
curl -fLo ~/.vim/colors/monokai.vim -S https://raw.githubusercontent.com/ErichDonGubler/vim-sublime-monokai/master/colors/sublimemonokai.vim
curl -fLo ~/.vim/colors/seoul256.vim -S https://raw.githubusercontent.com/junegunn/seoul256.vim/master/colors/seoul256.vim
curl -fLo ~/.vim/colors/sierra.vim -S https://raw.githubusercontent.com/AlessandroYorba/Sierra/master/colors/sierra.vim
curl -fLo ~/.vim/plugin/argtextobj.vim -S 'https://www.vim.org/scripts/download_script.php?src_id=11985'
ln -fns $(pwd)/config/after/compiler ~/.vim/after/compiler
ln -fns $(pwd)/config/compiler ~/.vim/compiler
ln -fns $(pwd)/config/filetype.vim ~/.vim/filetype.vim
(echo "0 0 * * * $(pwd)/backdrops.py"; echo "0 * * * * $(pwd)/power.sh") | crontab -
curl -fLo fonts-main.tar.gz -S https://github.com/google/fonts/archive/master.tar.gz
tar -xzf fonts-main.tar.gz
sudo mkdir -p $FONT_ROOT/google-fonts $FONT_ROOT/fira-code
find fonts-main -name "*.ttf" -exec sudo install -m644 {} $FONT_ROOT/google-fonts \;
rm -fr fonts-main fonts-main.tar.gz
for variant in Bold Light Medium Regular Retina SemiBold; do
  sudo curl -fLo $FONT_ROOT/fira-code/FiraCodeNerdFont-$variant.ttf -S https://github.com/ryanoasis/nerd-fonts/raw/master/patched-fonts/FiraCode/$variant/FiraCodeNerdFont-$variant.ttf
done
fc-cache -f
